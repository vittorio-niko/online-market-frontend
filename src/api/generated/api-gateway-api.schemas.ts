/**
 * Generated by orval v8.8.0 🍺
 * Do not edit manually.
 * API Gateway - Authentication Service
 * Authentication and authorization API for the marketplace platform. Handles user registration, login, token refresh, and logout.
 * OpenAPI spec version: 1.0.0
 */
/**
 * Request body for user registration
 */
export interface RegisterUserRequestDto {
  /**
     * User's first name
     * @minLength 1
     * @maxLength 100
     */
  name: string;
  /**
     * User's last name
     * @minLength 1
     * @maxLength 100
     */
  surname: string;
  /** Date of birth (must be in the past or present) */
  birthDate: string;
  /**
     * User's email address (used for login)
     * @maxLength 255
     */
  email: string;
  /** User's password (will be validated for strength) */
  password: string;
}

/**
 * Request body for user login
 */
export interface LoginRequestDto {
  /** User's email address */
  email: string;
  /** User's password */
  password: string;
}

/**
 * Response containing access token (refresh token is set as HTTP-only cookie)
 */
export interface LoginResponseDto {
  /** JWT access token (Bearer token for API authentication) */
  accessToken?: string;
  /**
     * Refresh token (deprecated - use HTTP-only cookie instead)
     * @nullable
     */
  refreshToken?: string | null;
}

/**
 * Machine-readable error code
 */
export type ErrorResponseCode = typeof ErrorResponseCode[keyof typeof ErrorResponseCode];


export const ErrorResponseCode = {
  RESOURCE_NOT_FOUND: 'RESOURCE_NOT_FOUND',
  EMAIL_DUPLICATE_CONFLICT: 'EMAIL_DUPLICATE_CONFLICT',
  PASSWORD_VALIDATION_ERROR: 'PASSWORD_VALIDATION_ERROR',
  INVALID_ARGUMENT: 'INVALID_ARGUMENT',
  INVALID_CREDENTIALS: 'INVALID_CREDENTIALS',
  INVALID_TOKEN: 'INVALID_TOKEN',
  FORBIDDEN: 'FORBIDDEN',
  UNAUTHORIZED: 'UNAUTHORIZED',
  AUTH_SERVICE_RESPONSE_ERROR: 'AUTH_SERVICE_RESPONSE_ERROR',
  AUTH_REGISTRATION_ERROR: 'AUTH_REGISTRATION_ERROR',
  WEB_CLIENT_ERROR: 'WEB_CLIENT_ERROR',
  INTERNAL_ERROR: 'INTERNAL_ERROR',
} as const;

/**
 * Map of field names to validation error messages (only for validation errors)
 * @nullable
 */
export type ErrorResponseFieldErrors = {[key: string]: string} | null;

/**
 * Standard error response format used across all endpoints
 */
export interface ErrorResponse {
  /** Machine-readable error code */
  code: ErrorResponseCode;
  /** Human-readable error description */
  message: string;
  /**
     * Map of field names to validation error messages (only for validation errors)
     * @nullable
     */
  fieldErrors?: ErrorResponseFieldErrors;
}

